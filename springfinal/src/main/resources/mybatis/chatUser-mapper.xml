<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="chatUser">

	<insert id="add">
	insert into chat_user(
		chat_room_no,
		member_id
	) values(
		#{chatRoomNo},
		#{memberId}
	)
	</insert>
	
	<select id="allList" resultType="ChatUserDto">
		select * from chat_user order by chat_room_no asc
	</select>

</mapper>